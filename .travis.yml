matrix:
  include:
    - language: java
      jdk: openjdk10
      before_install:
        - cd api
      deploy:
        provider: heroku
        api_key:
          secure: $HEROKU_API_KEY
        app: gulliver-api
    - language: node_js
      node_js:
        - 10
      before_install:
        - cd web
      script:
        - npm test
        - npm run build
